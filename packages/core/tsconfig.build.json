{
  // refs 层构建配置：仅 tsc -b 类型检查用，运行产物构建仍走 tsup（读 tsconfig.json）
  // core 依赖 foundation（路径/类型/配置基础层）
  "extends": ["./tsconfig.json", "../../tsconfig.refs.base.json"],
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist-types"
  },
  "references": [
    {
      "path": "../foundation/tsconfig.build.json"
    }
  ]
}

# Lattice 集成

通过 Lattice CLI 获取跨项目上下文管理能力。

## 推荐命令

- `lattice context`
- `lattice status`
- `lattice task list --current`
- `lattice search <query>`

## 建议工作流

1. 先读 `lattice context` 再改代码
2. 涉及跨项目经验时使用 `lattice search`
3. 涉及任务推进时查看 `lattice task list`
4. 发现长期规则时及时沉淀到 spec
